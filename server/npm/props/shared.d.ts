export interface InputProps {
    bind: string;
    type: string;
}
export declare const params: Params;
export declare const scenarioOf: (props: any, tag: any, type?: string, keys?: string[]) => any[];
